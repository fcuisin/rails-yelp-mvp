<h1>Add a review to: ***<%= @restaurant.name %>***</h1>

<%= simple_form_for([@restaurant, @review]) do |f| %>

    <%= f.input :content %>
    <%= f.input :rating, collection: Review::RATINGS %>

    <%= f.button :submit %>
<% end %>

<%= link_to 'Back to reviews', restaurant_path(@restaurant) %>
